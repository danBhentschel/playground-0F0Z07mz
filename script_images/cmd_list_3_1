Command[0]="truncate -s 3T /pool/disk1"
Comment[0]="Sparse file created. Now create the loop device."
Command[1]="sudo losetup loop0 /pool/disk1"
Comment[1]="Created a new block device called /dev/loop0"
Command[2]="sudo fdisk /dev/loop0"
Comment[2]="Now let's take a look at your new partitions"
Command[3]="sudo partprobe /dev/loop0"
Comment[3]="The OS should know about them now"
Command[4]="lsblk"
Comment[4]="Does that look correct?"
